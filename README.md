# Upclose Personal Timetable

A React/Vite web app for creating a personal festival timetable.

## Run locally

```bash
npm install
npm run dev
```

## Deploy to Vercel

Import this repository in Vercel and use the default Vite settings:

- Build command: `npm run build`
- Output directory: `dist`
